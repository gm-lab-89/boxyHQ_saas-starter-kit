import packageInfo from '../package.json';
import env from './env';

const app = {
  version: packageInfo.version,
  name: '',
  logoUrl: '/logo.svg',
  url: 'http://localhost:4002',
};

export default app;